// Create a program to find the length of string using strlen.
#include<stdio.h>
#include<string.h>
main()
{
	char S[100];
	printf("Enter a string:");
	gets(S);
	strlen(S);
	printf("%s",S);
}
